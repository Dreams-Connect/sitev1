import { ShorttimeagePipe } from './shorttimeage.pipe';

describe('ShorttimeagePipe', () => {
  it('create an instance', () => {
    const pipe = new ShorttimeagePipe();
    expect(pipe).toBeTruthy();
  });
});
